{
  "name": "madlib-app",
  "version": "1.0.0",
  "description": "Mad lib style app using react",
  "main": "server.js",
  "scripts": {
    "prestart": "npm run build",
    "start": "nodemon server",
    "test": "npm --prefix tester test",
    "dev:build": "npm run --prefix dist-builder dev:build",
    "build": "npm run --prefix dist-builder build"
  },
  "keywords": [
    "madlibs",
    "react"
  ],
  "author": "techmsi",
  "license": "ISC",
  "dependencies": {
    "express": "*",
    "isomorphic-fetch": "*",
    "react": "*",
    "react-dom": "*"
  },
  "devDependencies": {
    "babel-preset-env": "^1.7.0",
    "babel-preset-react": "^6.24.1"
  }
}
